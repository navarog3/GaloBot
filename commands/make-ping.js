module.exports = {
    name: 'make-ping',
    description: 'Creates a ping with the given name (no spaces pls).',
    usage: '[ping_name]',
    args: true,
    execute(message, args) {
      //message.channel.send('pong');
        
    },
  };